#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void main()
{
 int n, i, j ,temp, min;
 long start, stop;
 int a[50000];
 double time;
 
 printf("Enter the size of the array: ");
 scanf("%d", &n);
 
 for (i = 0; i < n; i++) 
 a[i] = rand() % 100;
 
 printf("Array elements to be sorted are:\n");
 for (i = 0; i < n; i++)
 printf("%d ", a[i]);
 
 start = clock();
 for (i = 0; i < n - 1; i++)
 {
 min = i;
 for (j = i + 1; j < n; j++) 
 {
 if (a[j] < a[min])
 min = j;
 }
 
 temp = a[min];
 a[min] = a[i];
 a[i] = temp;
 }
 stop = clock();
 pri
ntf("\nThe sorted elements are:\n");
 for (i = 0; i < n; i++)
 printf("%d ", a[i]);
 
 time=(stop - start)/(double) CLOCKS_PER_SEC;
 printf("\nThe time taken to sort is %lf millisec", time*1000);
 }
